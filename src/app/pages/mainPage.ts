import {Component, UI} from "../app/ui";

@Component({
    // language=Vue
    template: `
        <v-container fluid class="selectable">
          main page
        </v-container>
    `
})
export class MainPage extends UI {
}
